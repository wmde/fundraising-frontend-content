<html>
<script id="init-form" src="{$ basepath|e('html_attr') $}{$ '/res/js/donationForm.js'|prefix_file $}"
        data-initial-form-values="{% if initialFormValues %}{$ initialFormValues|json_encode|e('html_attr') $}{% else %}{}{% endif %}"
        data-violated-fields="{% if violatedFields %}{$ violatedFields|json_encode|e('html_attr') $}{% else %}{}{% endif %}"
        data-initial-validation-result="{% if validationResult %}{$ validationResult|json_encode|e('html_attr') $}{% else %}{}{% endif %}"
        data-validate-amount-url="{$ basepath|e('html_attr') $}/validate-payment-data"
        data-validate-address-url="{$ basepath|e('html_attr') $}/validate-address"
        data-validate-email-address-url="{$ basepath|e('html_attr') $}/validate-email"
        data-validate-iban-url="{$ basepath|e('html_attr') $}/check-iban"
        data-generate-iban-url="{$ basepath|e('html_attr') $}/generate-iban"
>
</script>

<div id="validation-errors" class="errorbox" style="display:none">
    <strong>Bei der Prüfung Ihrer Eingaben wurden Probleme bemerkt</strong>.
    Bitte überprüfen Sie Ihre Angaben in folgenden Feldern: <strong class="fields">keine</strong>
</div>

<div id="paymentPage">
    {% include 'DonationForm_Payment.html.twig' %}
</div>

<div id="personalDataPage">
    {% include 'DonationForm_PersonalData.html.twig' %}
</div>

<div id="bankConfirmationPage">
    {% include 'DonationForm_BankDataConfirmation.html.twig' %}
</div>

<div class="wlightbox-contents">

<div id="wlightbox-wohin-geht-das-geld">{% include '10h16_Lightbox_Wohin_geht_das_Geld.html.twig' %}</div>

<div id="wlightbox-steuerlich-absetzbar">{% include '10h16_Lightbox_Steuer.html.twig' %}</div>

<div id="wlightbox-bitcoin">{% include '10h16_Lightbox_BitCoin.html.twig' %}</div>

<div id="wlightbox-spendenkommentare">{% include '10h16_Lightbox_Spendenkommentare.html.twig' %}</div>

<div id="wlightbox-supporter-list" style="width: 686px;">{% include 'Supporter_List.html.twig' %}</div>

</div>
{% include 'Bank_Detail_Banner.html.twig' %}
</html>